name: base_module
description: Reusable base module with provider + dio + http
version: 0.0.1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  dio: ^5.4.3
  http: ^1.2.2
  shared_preferences: ^2.2.3
  image_picker: ^1.2.2
  file_picker: ^11.0.2
  go_router: ^17.2.3
